import { RegisterForm } from "@/features/Registration";

import styles from "./RegisterPage.module.scss";

const RegisterPage = () => {
  return <RegisterForm className={styles.registerPage} />;
};

export default RegisterPage;
